# Simpsons_Rule_Integrator
A local Python utility that computes the definite integral of a mathematical function using numerical approximation. The script dynamically handles user input for integration limits and the number of sub-intervals, adapting its execution logic to apply either Simpson's 1/3 Rule or Simpson's 3/8 Rule depending on the divisibility of the intervals.
